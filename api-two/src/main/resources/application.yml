# ==Tomcat����==
server:
  port: 8084
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8080/eureka/
spring:
  application:
    name: api-two
  main:
    allow-bean-definition-overriding: true
